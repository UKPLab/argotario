# StaticContent

This folder contains small HTML files that contain snippets which can be included somewhere.
To include a Component.XYZ.html file, use the <arg-component src="XYZ" /> directive.

Besides the components, this folder also contains the HTML files, which are included by the
StaticContent rounds, specified via the HTMLFile parameter.
